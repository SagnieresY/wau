<div class="invest-show-amount"> <%= nice_money(@investment.forecasted_amount) %></div>
<% if @investment.completed %>
	<div class="invest-show-small-text-completed"> <%= t("investments.show.info_bar.right_box.status_one") %></div>
<% else %>
	<div class="invest-show-small-text"> <%= t("investments.show.info_bar.right_box.status_two") %></div>
<% end %>