<%= content_for(:after_js) do %>

<script>
//NGO CHART
  // unlocked amount per ngo
  let unlocked_amount_ngo  = {
    x: <%= raw @chart_ngo_data[:unlocked].keys %> ,
    y: <%= raw @chart_ngo_data[:unlocked].values %> ,
    name: 'unlocked amount',
    type: 'bar'
  };
  // unlocked amount per ngo
  let locked_amount_ngo = {
    x: <%= raw @chart_ngo_data[:locked].keys %>,
    y: <%= raw @chart_ngo_data[:locked].values %>,
    name: 'locked amount',
    type: 'bar'
  };

  var data = [unlocked_amount_ngo, locked_amount_ngo];

  var layout = {barmode: 'stack'};

  Plotly.newPlot('ngo', data, layout);
</script>

<% end %>
